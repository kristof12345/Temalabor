<Page
    x:Class="Desktop.Views.MyReservationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Desktop.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding MyReservationsViewModel, Source={StaticResource Locator}}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock Style="{ThemeResource BaseTextBlockStyle}" Visibility="{x:Bind ViewModel.HasReservation}" Text="You don't have any reservations. To add a reservation click New Reservation." Margin="100,100,0,0" Width="350" TextWrapping="Wrap" HorizontalAlignment="Left"/>
        <ListView x:Name="listView"
                  Margin="0,0,1000,350"
                  SelectionMode="Single"
                  SelectionChanged="listView_SelectionChanged"
                  ScrollViewer.HorizontalScrollMode="Enabled">
        </ListView>
        <Button Content="New Reservation" HorizontalAlignment="Left" Margin="180,600,0,0" VerticalAlignment="Top" Click="Button_Click"/>

    </Grid>
</Page>
