<Page
    x:Class="Desktop.Views.PlanePage"
    x:Name="PlaneDeckPlan"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Desktop.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:usercontrols="using:Desktop.UserControls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid x:Name="grid">
        <Button x:Name="btPay" Content="Pay" HorizontalAlignment="Left" Margin="25,200,0,0" VerticalAlignment="Top" Click="Pay_Button_Click" Width="100"/>
        <Image x:Name="planeImg" HorizontalAlignment="Right" Width="1000" Height="700"/>
        <Canvas x:Name="myList" Width="1000" HorizontalAlignment="Right" Background="Transparent" Height="700"/>
        <TextBlock x:Name="txDetails"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="25,0,0,0"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Left"
        />
        <TextBlock x:Name="txPrice"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="25,100,0,0"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Left"
        />
        
    </Grid>
</Page>
